import React from 'react';
import SplashScreen from './SplashScreen';

// Loading screen for Suspense fallback (initial app load)
const PageLoader: React.FC = () => <SplashScreen />;

export default PageLoader;

